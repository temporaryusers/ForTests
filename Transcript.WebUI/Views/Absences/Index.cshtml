@model IEnumerable<Transcript.Domain.Entities.Absence>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Student.SecondName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subject.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Teacher.SecondName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.TypeClass.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DateAbsence)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Student.SecondName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subject.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Teacher.SecondName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TypeClass.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DateAbsence)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
